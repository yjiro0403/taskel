# QA Test Sheet - Integration & System Testing

| Test ID | Category | Test Case | Pre-conditions | Steps | Expected Result |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **ROUT-01** | Routines | Add Metadata to Routine | Authenticated | 1. Open Routine Modal.<br>2. Fill Title, Project, Tags, and Memo.<br>3. Save. | Routine is saved with all 4 metadata fields visible on edit. |
| **ROUT-02** | Routines | Propagation to Task | ROUT-01 complete | 1. Trigger routine generation for today.<br>2. Click on the generated task. | Task should have the same Project, Tags, and Memo as the Routine. |
| **TASK-01** | Tasks | Date Consistency | Tasks exist for multiple dates | 1. Create tasks for 2026-01-12 and 2026-01-13.<br>2. Switch between dates in UI. | Only tasks belonging to the selected date are shown. |
| **PERM-01** | Auth | Unauthorized Access | Logged out or different user | 1. Attempt to access a project URL of another user. | Firestore should deny access (Verified via console/UI error handling). |
| **ONBD-01** | Onboarding | First-time Flow | New user or cleared localStorage | 1. Login/Visit landing page. | User Guide/Intro tasks are triggered via `/api/onboarding`. |
| **ONBD-02** | Onboarding | Persistence | ONBD-01 complete | 1. Refresh page or login again. | Intro tasks/User Guide should NOT reappear unnecessarily. |
| **UTIL-01** | Data | Firestore Sanitization | Developer tools open | 1. Create a task with an empty description/optional field. | No `undefined` field errors in console/Firestore. |
| **VIRT-01** | Virtual | Infinite Future Projection | Routine exists | 1. Go to Calendar.<br>2. Move 1 year into the future.<br>3. Select a date. | Virtual tasks from routines appear correctly. |
| **VIRT-02** | Virtual | Instantiation on Action | Virtual task visible | 1. Click "Play" on a virtual task.<br>2. Refresh page. | Task is now a real task in DB and state is persisted (in_progress/done). |
| **VIRT-03** | Virtual | Deduplication | Virtual task visible | 1. Edit a virtual task (title).<br>2. Confirm display. | Only the edited (real) task is shown; the virtual one is hidden by the same ID. |
| **VIRT-04** | Virtual | Routine Deletion Sync | Routine and virtual tasks exist | 1. Delete a Routine. | All associated virtual tasks for all future dates disappear immediately. |
| **TASK-02** | Tasks | Sorting by Scheduled Start | Tasks exist in section | 1. Set Task A time "10:00".<br>2. Set Task B time "09:00". | Task B appears before Task A in the list. |
| **TASK-03** | Mobile/UI | Tag Selection via Enter | Mobile Soft Keyboard | 1. Open Add Task/Routine Modal.<br>2. Type tag.<br>3. Press Enter on keypad. | Tag is added as a chip. Form does NOT submit. Focus remains. |
| **TASK-04** | Tasks | Task Duplication | Task exists | 1. Locate task.<br>2. Click Duplicate (Copy icon left of time). | A new task with "(copy)" suffix is created in the same section with same metadata. Finance rows are **not** copied. |
| **FIN-01** | Finance | Default off | Authenticated | 1. Open Settings → General.<br>2. Confirm money tracking is OFF.<br>3. Open a task and the planning header. | No finance UI, no totals, existing Taskel behavior unchanged. |
| **FIN-02** | Finance | Record + totals | FIN-01, toggle ON | 1. Add a 20,000 expense and 10,000 income on a dated task.<br>2. Save.<br>3. Click the daily/planning summary. | Totals match. Breakdown is grouped by type and category and stays private to the user. |
| **NOWNEXT-01** | Now/Next widget | Running task countdown | Authenticated, today has a task with a 30 min estimate | 1. Open `/tasks`.<br>2. Press ▶ on the task.<br>3. Watch the widget above the list. | "今" shows the task title and `残り 30分`, the end time (`HH:mm まで`), and the number decreases every minute (every second under 10 min). |
| **NOWNEXT-02** | Now/Next widget | Overrun and no-estimate cases | NOWNEXT-01 | 1. Let a 1 min task run past its estimate.<br>2. Start a task whose estimate is 0. | Past the estimate the reading turns red `超過 X`. A 0-estimate task shows `経過 X` and no end time. Most recently started timer is shown; others appear as `+N 件実行中`. |
| **NOWNEXT-03** | Now/Next widget | Next fixed-time countdown | Today has an open task with start time 3 min from now (e.g. "バス") | 1. Open `/tasks`.<br>2. Wait. | "次" shows `HH:mm 開始` and `あと 3分`, turns red with a pulsing dot within 5 min, counts seconds under 10 min, and reads `今すぐ` once the start time arrives (still shown while another task is running). After the task's window (estimate, min 1 min) passes it drops out. |
| **NOWNEXT-04** | Now/Next widget | Conflict hint and queue fallback | Running task ends 10:50; open task scheduled 10:30 | 1. Observe the widget.<br>2. Mark the 10:30 task done. | Step 1: amber `次の予定のほうが先に来ます` under "今". Step 2: with no fixed-time task left, "次" shows the first open unscheduled task in list order with `時刻未設定`; with nothing left, `この後の予定はありません`. |
| **NOWNEXT-05** | Now/Next widget | Today-only, jump, collapse | Tasks exist on today and yesterday | 1. Navigate to yesterday in the date bar.<br>2. Tap the task title in the widget.<br>3. Collapse via the chevron and reload. | Widget keeps showing today's now/next while yesterday is displayed. Tapping the title switches the list to today and highlights the task. Collapsed one-line state persists after reload. |
| **WIDGET-01** | Home widget | Add widget + first sync | Widget-enabled APK installed, logged in, task list shown | 1. Long-press home → Widgets → Taskel → place "Taskel 今 / 次".<br>2. Return to the app, press ▶ on a 30 min task, go back to home. | Widget shows the task title, `残り` with a Chronometer counting down from 30:00, `HH:mm まで`, and `更新 HH:mm`. Before the first sync it reads `Taskel を開くと同期されます`. |
| **WIDGET-02** | Home widget | Countdown and transitions without the app | WIDGET-01; an open task scheduled 2 min from now with a 1 min estimate | 1. Close the app completely.<br>2. Watch the widget for 4 minutes. | `次` shows `HH:mm 開始` and a countdown in red; at the start time it flips to `今すぐ`; one minute later it advances to the following task (or `この後の予定はありません`), all while the app is closed. |
| **WIDGET-03** | Home widget | Overrun and no-estimate | WIDGET-01 | 1. Start a 1 min task and wait 2 min with the app closed.<br>2. Start a 0-estimate task. | Step 1: `超過` in red with a Chronometer counting up from the planned end. Step 2: `経過` counting up from the start, no end time. |
| **WIDGET-04** | Home widget | Change propagation and logout | WIDGET-01 | 1. In the app mark the running task done.<br>2. Add a task with a start time later today.<br>3. Log out. | Steps 1-2 update the widget immediately while the app is open. Step 3 clears it to `Taskel を開くと同期されます`. |
| **WIDGET-05** | Home widget | Tap, dark mode, reboot | WIDGET-01 | 1. Tap the widget.<br>2. Toggle system dark theme.<br>3. Reboot the device. | Tap opens Taskel. Dark theme switches to the night palette. After reboot the widget still shows the last snapshot and countdowns keep running. |
| **WKTL-01** | Weekly timeline | Title-first blocks | Planning → Weekly, a 15 min and a 60 min task with start times exist | 1. Open the weekly planning tab.<br>2. Look at both blocks. | Each block starts with its title (no `HH:mm–HH:mm` inside the block); the 15 min block shows one truncated line, the 60 min block wraps to two lines. Hovering shows the full title. The daily `/tasks` timeline shows the title first with the range in small text on the right. |
| **WKTL-02** | Weekly timeline | Move a scheduled block to another day and time | Wednesday has a task at 09:00 (60 min) | 1. Drag the block from Wednesday onto Thursday at 10:00.<br>2. Release. | While hovering, Wednesday shows the block faded in place and Thursday shows a dashed preview with the title at 10:00. After release the task is on Thursday 10:00–11:00, its section matches 10:00, and the change persists after reload. |
| **WKTL-03** | Weekly timeline | Move a no-start-time task to another day | Wednesday has a task without a start time | 1. Drag the chip from Wednesday's "No start time" area onto Thursday's "No start time" area (hint "Drop here to move to this day" appears).<br>2. Release.<br>3. Drag another chip from Wednesday onto Thursday's grid at 14:00. | Step 2: the task is now listed under Thursday's "No start time", still without a start time. Step 3: the task is on Thursday at 14:00. |
| **WKTL-04** | Weekly timeline | Clear a start time by dropping on the area | Thursday has a task at 10:00 | 1. Drag the block onto any "No start time" area (own day or another day; hint "Drop here to clear the start time" appears).<br>2. Release. | The task moves to that day's "No start time" area with no start time; reload keeps it unscheduled. Dragging inside the own grid still just changes the time. |
| **WKTL-05** | Weekly timeline | Same-day behavior unchanged | WKTL-02 | 1. Drag a block onto the right-hand notes panel and release.<br>2. Drag a block within its own day and release.<br>3. Click a block without moving. | Step 1: the task stays on its own day (only its time may change, as before) and no preview is left on any column. Step 2: only the start time changes (no date change). Step 3: the editor opens. |
| **TLINE-01** | Timeline (mobile) | Empty tap does not change the date | Phone/iPad, viewing yesterday, Now/Next widget visible | 1. Tap the empty space right of the widget title and of the countdown.<br>2. Tap the title text itself. | Step 1: the viewed date stays. Step 2: the list switches to today and highlights the task (unchanged behaviour). |
| **TLINE-02** | Timeline (mobile) | Long-press drag | Phone, timeline view, a block at 10:00 | 1. Swipe over a block without pausing.<br>2. Press and hold a block ~0.3 s (it lifts with a blue ring), drag to 12:00, release.<br>3. Drag a block toward the bottom edge of the screen. | Step 1: the page scrolls, nothing moves. Step 2: the block lands at 12:00, no editor opens. Step 3: the timeline scrolls while the preview follows the finger. A plain tap still opens the editor. |
| **TLINE-03** | Timeline | Actual time linkage | Timeline view, task planned 12:00 (60 min) | 1. Press ▶ at 11:30.<br>2. Press ■ at 11:50.<br>3. Switch to the section list view, press ▶ on a task planned for 17:00 at 15:30, switch back to the timeline. | Step 1: the block moves to 11:30–12:30 and is running. Step 2: the block is done and spans 11:30–11:50. Step 3: the timeline draws the running block from 15:30 growing to "now" even though the list view left the planned time at 17:00; dragging that block moves its real start. |
| **TLINE-04** | Timeline | Create from an empty slot | Timeline view (daily and weekly) | 1. Hover an empty slot (desktop) — a dashed "+ HH:mm" slot follows the mouse.<br>2. Click at 15:07 (or tap on mobile).<br>3. Save with a title. | The create form opens with the day and 15:00 preset and the matching section; the new task appears at 15:00. Clicking a block opens the editor, not the create form. |
| **TLINE-05** | Timeline | Sectioned unscheduled area and start now | Two no-start-time tasks in different sections | 1. Look at the "No start time" area.<br>2. Press ▶ on a chip.<br>3. Drag the other chip onto a different section group (all sections appear as slots while dragging). | Step 1: chips are grouped under their section names with the section's hours. Step 2: the task appears on the grid at the current time, running. Step 3: the chip moves under the target section and keeps no start time. |
| **TLINE-09** | Timeline | Start then stop within seconds | A no-start-time task (estimate 30 min) | 1. Press ▶ on the chip at 07:03.<br>2. Press ■ about 20 seconds later.<br>3. Repeat with a 3-minute run. | Step 2: the done block reads `07:03–07:04` (one minute is logged, not 30 planned minutes) and keeps its minimum height. Step 3: the label ends at the real stop time (e.g. `07:10–07:13`), not 15 minutes after the start. |
| **TLINE-06** | Timeline | Copy and Google Calendar link on blocks | A synced Google Calendar task and a normal task | 1. Hover a block and press the copy icon (always visible on touch).<br>2. Press the ↗ icon on the synced task. | Step 1: a "(copy)" task appears at the same time; the editor does not open. Step 2: the Google Calendar event opens in a new tab; nothing else happens on the timeline. |
| **TLINE-08** | Timeline (weekly) | Routine task on another day | A daily routine at 17:00 exists; planning → Weekly, viewing a day other than the selected daily date | 1. Press ▶ on the routine's block on Thursday.<br>2. Drag it to another time. | Both persist (the routine occurrence is materialized with the real start / new time); nothing is silently ignored. |
| **TLINE-10** | Timeline | Delete from the edit modal | Timeline view with a normal task and a routine occurrence | 1. Click the normal task's block, press 削除, confirm.<br>2. Click the routine occurrence, press 今回分を削除, confirm.<br>3. Cancel a confirmation once. | Step 1: the task disappears from the timeline and the list view. Step 2: only today's occurrence disappears; the routine and its other days stay. Step 3: nothing changes and the modal stays open. |
| **TLINE-07** | Timeline (daily) | Drop targets on the daily page | Daily timeline, one block, one chip in the morning section | 1. Drag the block onto the "No start time" area.<br>2. Drag the chip onto the "Work" section group. | Step 1: the block becomes a chip (start time cleared). Step 2: the chip is listed under "Work" (section changed). Same-day time changes still work as before. |
